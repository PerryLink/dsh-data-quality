packages:
  - .

nodeLinker: hoisted
onlyBuiltDependencies:
  - esbuild
# The 0.1.1-rc.2 peer family was published inside the registry's
# minimum-release-age window; exclude the scope so installs resolve.
minimumReleaseAgeExclude:
  - '@deepseek-ai/*'
allowBuilds:
  esbuild: true
